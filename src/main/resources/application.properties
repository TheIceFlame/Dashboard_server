spring.application.name=samah

server.port=8080

# Database Configuration
spring.datasource.url=jdbc:postgresql://dpg-cree1hij1k6c73dcmmr0-a.oregon-postgres.render.com:5432/dashboard_132o
spring.datasource.username=dashboard_132o_user
spring.datasource.password=JR0EiZ8oQ9p1ZKzigk9PioLtigVx0ZiO

# Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.PostgreSQLDialect
spring.jpa.hibernate.ddl-auto=update

# Logging Configuration
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE

# Multipart Configuration
spring.servlet.multipart.enabled=true
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# DataSource Initialization
spring.datasource.initialization-mode=always

# Security and CORS Configuration
# Optional: Uncomment and configure if needed for CORS and security
# spring.web.cors.allowed-origins=http://your-client-url.com
# spring.web.cors.allowed-methods=GET,POST,PUT,DELETE
# spring.web.cors.allowed-headers=*

# Health Checks (Optional: Adjust or add if needed for Render's health checks)
management.endpoints.web.exposure.include=health,info
management.endpoint.health.show-details=always
